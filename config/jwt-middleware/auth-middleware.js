// const expressJWT = require('express-jwt');

// // user express-jwt package to create jwt middleware that verifies jwt
// // add this middleware when defining routes
// const jwtMiddleware = expressJWT({
//   secret: process.env.JWT_SECRET, // get jwt secret from environment variable
// });

// // console.log(jwtMiddleware, "my secret");

// module.exports = jwtMiddleware;